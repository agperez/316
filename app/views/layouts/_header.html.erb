    <div class="navbar navbar-default navbar-fixed-top" role="navigation">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <div class="navbar-brand">
            <%= link_to(root_path) do %>
              C316.<em>tv</em>
            <% end %>
          </div>
        </div>
        <div class="navbar-collapse collapse">
          <ul class="nav navbar-nav">
            <% unless @sermons_recent.nil? %>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">Recent Sermons <b class="caret"></b></a>
                <ul class="dropdown-menu">
                  <% @sermons_recent.each do |sermon| %>
                    <li><a href="<%= sermon_path(sermon) %>"><%= sermon.s_date.strftime("%m/%d/%Y") %> | <%= sermon_title(sermon) %></a>
                    </li>
                  <% end %>
                </ul>
              </li>
              <li><%= link_to "Sermon Archive", archive_path %></li>
            <% end %>

          </ul>
          <ul class="nav navbar-nav navbar-right">
            <li><%= link_to "theChurch", us_path, id: "the_church_link" %></li>
            <% if signed_in? %>
              <% if current_user.admin? %>
                <li id="fat-menu" class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                    Admin <b class="caret"></b>
                  </a>
                  <ul class="dropdown-menu">
                    <li><%= link_to "Dashboard", dashboard_path %></li>
                    <li><%= link_to "Calendar", calendar_path %></li>
                  </ul>
                </li>
                <li id="fat-menu" class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                    My Pages <b class="caret"></b>
                  </a>
                  <ul class="dropdown-menu">
                    <li><%= link_to "My Profile", profile_user_path(current_user) %></li>
                    <li><%= link_to "My Schedule", current_user %></li>
                    <li><%= link_to "Edit My Settings", edit_user_path(current_user) %></li>
                    <li><%= link_to "Change My Picture", picture_user_path(current_user) %></li>
                    <li class="divider"></li>
                    <li><%= link_to "Sign out", signout_path, method: "delete" %></li>
                  </ul>
                </li>
              <% else %>
                <li id="fat-menu" class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                    My Pages <b class="caret"></b>
                  </a>
                  <ul class="dropdown-menu">
                    <li><%= link_to "My Profile", profile_user_path(current_user) %></li>
                    <li><%= link_to "My Schedule", current_user %></li>
                    <li><%= link_to "Settings", edit_user_path(current_user) %></li>
                    <li class="divider"></li>
                    <li><%= link_to "Sign out", signout_path, method: "delete" %></li>
                  </ul>
                </li>
              <% end %>
            <% else %>
              <li><%= link_to "Sign in", signin_path %></li>
            <% end %>
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </div>
