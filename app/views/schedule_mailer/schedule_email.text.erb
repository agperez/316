<% if @events.length == 0 %>
        There are currently have no upcoming dates where you're scheduled to serve. 
      <% else %>
        <% @events.each do |event| %>
          <%=event.event_date.strftime("%B %d, %Y")%> - <%= event.role.name %>
        <% end %>
      <% end %>